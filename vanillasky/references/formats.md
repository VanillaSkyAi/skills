# Formats — the slot contract as data

> Generated by `npm run gen:registry` from `src/lib/format-definitions.ts` — do not edit.
> Declare the format in the config (`"format": "launch"`) or pass `--format launch`
> to `vanillasky validate` / `render` to enforce these rules mechanically.

## launch

Tech/SaaS launch moment: hook → 3 bodies (framing, comprehension, proof) → closer. Default 5 scenes at each template's preferredDuration, typically 15-20s total. 4 scenes only for explicitly simple/short input; bodies grow beyond 3 only when the user provides 4+ specific assets that each deserve their own scene (cap 6 bodies).

- **Scenes**: 3–8, default 5. First scene is the hook, last is the closer, everything between is a body.
- **Hook**: default template `media`. Banned as hook (motion too slow to land in the first 2–3s): `chatWhatsapp`, `chatMessenger`, `appFeed`, `problemSolution`, `beforeAfter`.
- **Bodies**: 1–6, default 3. Role budget for the default build — one body per role: framing, comprehension, proof.
- **Closer**: the last scene must use a `closer`-category template, and `closer` templates may appear ONLY in the final slot.
- **Diversity**: adjacent scenes must come from different template categories; adjacent scenes must not use the same template. Exempt categories: `media` (multi-upload carve-out — distinct assets or deliberately different mediaKeywords).

## Template categories

The category map the diversity + closer rules run against:

- **media**: `media`, `reaction`
- **celebration**: `confetti`, `emojiBurst`
- **data**: `bigNumber`, `barChart`, `progressRing`, `tripleStats`
- **device**: `phoneMockup`, `webMockup`, `codeEditor`, `terminal`
- **social**: `tweet`, `notification`, `chatMessenger`, `chatWhatsapp`, `milestone`, `reviewStack`, `testimonial`, `incomingCall`, `brandMessage`, `promptInput`
- **explainer**: `beforeAfter`, `problemSolution`, `cardList`, `steps`
- **closer**: `ctaLogo`, `ctaMedia`
